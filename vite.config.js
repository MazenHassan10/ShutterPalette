import { defineConfig } from "vite";

export default defineConfig({
  root: "./", // The directory where your index.html is located
  build: {
    outDir: "dist", // The directory where the build files will go
  },
});
